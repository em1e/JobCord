"""Scraper package init.

This file makes the `scraper` folder importable as a Python package so scripts
and the bot can import modules like `scraper.scrape_manager` or
`scraper.remoteok_scraper` when running from the repository root.
"""

# package initializer (intentionally minimal)
